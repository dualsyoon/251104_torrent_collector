# 문제 해결 가이드

## 🚨 연결 오류 (ConnectionResetError)

### 증상
```
ConnectionResetError(10054, '현재 연결은 원격 호스트에 의해 강제로 끊겼습니다')
```

### 원인
1. **ISP 차단**: 한국의 일부 ISP(통신사)가 성인 토렌트 사이트를 차단
2. **방화벽**: 회사, 학교, 공공 와이파이의 방화벽
3. **사이트 다운**: 해당 사이트가 일시적으로 다운

### 해결 방법

#### ✅ 방법 1: VPN 사용 (가장 효과적)

**무료 VPN 추천:**
- **ProtonVPN**: https://protonvpn.com/
- **Windscribe**: https://windscribe.com/
- **TunnelBear**: https://www.tunnelbear.com/

**설정 방법:**
1. VPN 설치
2. 다른 국가 서버 선택 (일본, 미국 추천)
3. VPN 연결 후 애플리케이션 실행

#### ✅ 방법 2: CloudScraper 라이브러리 설치

```bash
pip install cloudscraper
```

이미 코드에 적용되어 있으며, 설치하면 자동으로 활성화됩니다.

#### ✅ 방법 3: 연결 테스트

```bash
python test_connection.py
```

이 스크립트로 어떤 사이트가 접속 가능한지 확인하세요.

#### ✅ 방법 4: 다른 네트워크 시도

- 모바일 핫스팟 사용
- 다른 와이파이 네트워크
- 유선 인터넷 ↔ 무선 인터넷 전환

#### ✅ 방법 5: 프록시 설정 (고급)

프록시 서버를 통해 접속:

```python
# scrapers/base_scraper.py 수정
proxies = {
    'http': 'http://프록시주소:포트',
    'https': 'http://프록시주소:포트',
}
response = self.session.get(url, proxies=proxies, ...)
```

---

## 🔧 기타 문제

### SSL 오류
```bash
pip install --upgrade certifi
pip install --upgrade urllib3
```

### 패키지 설치 오류
```bash
pip install --upgrade pip
pip install -r requirements.txt --force-reinstall
```

### 데이터베이스 오류
```bash
# torrents.db 파일 삭제 후 재시작
```

---

## 📞 여전히 안 될 때

1. **GitHub Issues** 확인
2. **VPN 필수 사용** - 대부분의 문제가 해결됩니다
3. **다른 토렌트 사이트** 추가 요청

---

## 🌍 국가별 접속 가능성

| 국가 | Sukebei | JAVTorrent | TorrentKitty | 권장사항 |
|------|---------|------------|--------------|----------|
| 🇰🇷 한국 | ❌ 차단 | ❌ 차단 | ❌ 차단 | **VPN 필수** |
| 🇯🇵 일본 | ✅ | ✅ | ✅ | VPN 권장 |
| 🇺🇸 미국 | ✅ | ✅ | ✅ | 직접 접속 가능 |
| 🇨🇳 중국 | ❌ 차단 | ❌ 차단 | ❌ 차단 | VPN 필수 |

**한국에서는 VPN 사용이 거의 필수입니다.**

