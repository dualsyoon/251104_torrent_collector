# 토렌트 수집기 리팩토링 계획 (동작 보존)

이 문서는 **현재 동작을 바꾸지 않으면서** 코드베이스의 유지보수성/안정성을 개선하기 위한 단계별 리팩토링 계획입니다.

## 0. 원칙 (반드시 지킴)

- **기능/동작 동일**: UI 동작, DB 스키마/마이그레이션, 스크래핑/이미지 검색 로직, 설정(QSettings/.env), 출력 로그(중요한 print)는 바꾸지 않는다.
- **작게 쪼개서 진행**: 한 번에 한 모듈/한 책임만 건드린다.
- **매 단계 검증**: 단계마다 import/컴파일 스모크 + (가능하면) 샘플 데이터로 GUI 기동 확인.
- **롤백 가능**: 각 단계는 독립 커밋/변경 단위로 되돌릴 수 있게 한다.

## 1. 현재 구조/핵심 흐름 (요약)

- `main.py`: GUI 실행 + Playwright 서버 워밍업/종료 정리.
- `gui/main_window.py`: 앱의 대부분 로직(스크래핑 시작/중단, DBWriter, 썸네일 백그라운드 처리, 교체 큐, 시스템 트레이, 페이지네이션)을 포함.
- `gui/torrent_list.py`: 테이블 렌더링 + 썸네일 lazy loading + hover preview.
- `scrapers/*`: Selenium 기반 스크래퍼/서버 및 `ImageFinder`(썸네일 검색)가 매우 비대.
- `database/*`: SQLAlchemy 모델/쿼리 + `DBWriterThread`(단일 쓰기 스레드).

## 2. 가드레일(동작 보존 장치)

- **스모크 스크립트**: `python scripts/smoke.py`
  - `Database(':memory:')`로 모델/마이그레이션/초기 데이터가 깨지지 않는지 확인
  - 전체 파이썬 파일 `compileall`로 문법/임포트 오류 조기 탐지

> GUI 런타임 동작은 환경(VPN/selenium/브라우저)에 의존하므로, 자동화보다는 “최소 스모크 + 샘플 데이터 기동”이 현실적인 가드레일입니다.

## 3. 단계별 리팩토링 로드맵

### 단계 A (저위험/순수 리팩토링)

- 목표: “파일 비대/중복/의존성 꼬임”을 **구조적으로만 정리** (로직 변경 없음)
- 작업:
  - `gui/main_window.py`에서 QThread 워커 클래스들을 `gui/workers.py`(또는 `gui/workers/*`)로 분리
  - `gui/torrent_list.py`에서 UI 전용 코드 vs 이미지 로딩/캐시 코드를 분리(예: `image_loader.py`와 책임 재정렬)
  - `scrapers/image_finder.py`에서 “공통 HTTP/필터링/차단 감지” 유틸을 내부 클래스로 정리 (외부 API는 그대로)

검증:
- `python scripts/smoke.py`
- (가능하면) `python add_sample_data.py` 후 `python main.py`로 기동/기본 탐색

### 단계 B (모듈 경계/의존성 정리)

- 목표: GUI가 스크래핑/이미지 탐색/DB 저장 디테일을 과도하게 알지 않도록 완화
- 작업(동작 동일 조건):
  - `MainWindow` → “서비스 객체(예: TorrentService)”에 작업 위임
  - 워커(QThread) ↔ 서비스 ↔ DBWriter 경계 정리 (시그널 payload는 동일 유지)

검증:
- 단계 A와 동일 + 주요 버튼 플로우(수집/중단/정렬/썸네일 교체) 수동 확인

### 단계 C (안정성/관측성 개선)

- 목표: 예외 처리/타임아웃/재시도/로그 일관성 개선(동작은 동일)
- 작업:
  - print 남발을 “동일한 메시지”를 내는 로거/헬퍼로 통일 (출력 메시지 유지)
  - 네트워크 요청에 공통 timeout/재시도 정책 정리(기존 값/의미 유지)
  - 스레드 종료/정리 시나리오 정리(앱 종료 시 DBWriter/Playwright 정리)

검증:
- 스모크 + 장시간 실행 시 메모리/스레드 누수 관찰

## 4. 롤백 포인트

- 단계 A/B/C 각 단계는 독립 커밋 단위로 관리
- 문제 발생 시: 마지막 단계만 되돌려도 앱이 동작하도록 “기존 인터페이스 유지”를 최우선으로 함


